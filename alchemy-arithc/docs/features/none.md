# None

The `None` literal represents the absence of a value.

## Literal

```
print None
```
Output: `None`

## Default Return Value

Functions that reach the end without a `return` evaluate to `None`.

## Comparisons

`None` can be compared with `==` and `!=`.
