# Ackermann function - famous recursive function
def ackermann(m, n) (
  if m == 0 then
    return n + 1
  else (
    if n == 0 then
      return ackermann(m - 1, 1)
    else
      return ackermann(m - 1, ackermann(m, n - 1))
  )
)

# Warning: grows very fast!
print ackermann(0, 0)
print ackermann(1, 2)
print ackermann(2, 2)
print ackermann(3, 2)
