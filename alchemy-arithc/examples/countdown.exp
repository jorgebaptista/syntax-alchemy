# Countdown and FizzBuzz style
def fizzbuzz(n) (
  if n % 15 == 0 then
    return 0
  else (
    if n % 3 == 0 then
      return 1
    else (
      if n % 5 == 0 then
        return 2
      else
        return n
    )
  )
)

# Print fizzbuzz pattern for 1 to 20
set i = 1
while i <= 20 do
  print fizzbuzz(i)
  set i = i + 1
done
